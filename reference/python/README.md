# Python reference implementation

- `emtp.py` provides minimal deterministic canonicalization + variant expansion + HMAC token generation.
- For production use, implement full address normalization + robust nickname handling + input validation.
